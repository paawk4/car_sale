<Page x:Class="car_sale.DilerList"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:car_sale"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="580"
      Title="DilerList">

    <Grid Background="White">
        <Button x:Name="CreateDiler_Button" Content="Добавить дилера" HorizontalAlignment="Left" Margin="36,30,0,0" VerticalAlignment="Top" Width="100" Click="CreateDiler_Button_Click"/>
        <ListView x:Name="listDilers" Margin="36,66,36,26" MouseDoubleClick="listDilers_MouseDoubleClick">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="id" DisplayMemberBinding="{Binding id}"/>
                    <GridViewColumn Header="Дилер" DisplayMemberBinding="{Binding diler}"/>
                    <GridViewColumn Header="Адрес" DisplayMemberBinding="{Binding address}"/>
                    <GridViewColumn Header="Телефон" DisplayMemberBinding="{Binding phone}"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Page>
