<Page x:Class="car_sale.CarList"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:car_sale"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="580"
      Title="CarList">

    <Grid Background="White">
        <Button x:Name="CreateCar_Button" Content="Добавить авто" HorizontalAlignment="Left" Margin="36,30,0,0" VerticalAlignment="Top" Width="100" Click="CreateCar_Button_Click"/>
        <ListView x:Name="listCars" d:ItemsSource="{d:SampleData ItemCount=5}" Margin="36,66,36,26" MouseDoubleClick="ListCars_MouseDoubleClick">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="id" DisplayMemberBinding="{Binding id}"/>
                    <GridViewColumn Header="Авто" DisplayMemberBinding="{Binding car}"/>
                    <GridViewColumn Header="Пробег" DisplayMemberBinding="{Binding mileage}"/>
                    <GridViewColumn Header="Дилер" DisplayMemberBinding="{Binding diler}"/>
                    <GridViewColumn Header="Стоимость" DisplayMemberBinding="{Binding price}"/>
                </GridView>
            </ListView.View>
        </ListView>

    </Grid>
</Page>
